# Copy to .env (gitignored) and fill in values.
# docker-compose auto-loads .env from the project root.

# GitHub personal access token — gives the agent `gh` CLI access.
# Minimum scopes: repo, read:org
# Create at: https://github.com/settings/tokens
GITHUB_TOKEN=ghp_your_token_here
