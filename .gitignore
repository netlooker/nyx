# Secrets — never commit
secrets/

# Claude Code local config — personal, not for sharing
.claude/

# Runtime workspaces (git worktrees, ephemeral per task)
workspaces/

# Nix build outputs
result
result-*

# Python
__pycache__/
*.pyc
.venv/
*.egg-info/

# direnv
.direnv/

# Runtime data (openclaw state — all gitignored)
data/
agents/
sandboxes/
